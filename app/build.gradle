apply plugin: 'com.android.application'

android {
    signingConfigs {
        westvansurfreport {
            keyAlias 'WVS'
            keyPassword '1Tangynorwvs'
            storeFile file('C:/Dropbox/Development/Android_Studio/Keystore/WestVanSurfReport Keystore/westvansurfkey.jks')
            storePassword '1Tabbynorwvs'
        }
    }
    compileSdkVersion 25
    buildToolsVersion '25.0.2'
    defaultConfig {
        applicationId "com.rsnorrena.westvansurfreport"
        minSdkVersion 19
        targetSdkVersion 25
        versionCode 9
        versionName '9'
        signingConfig signingConfigs.westvansurfreport
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_7
        targetCompatibility JavaVersion.VERSION_1_7
    }
    productFlavors {
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile files('libs/jsoup-1.8.1.jar')
    compile files('libs/activation.jar')
    compile files('libs/mail.jar')
    compile files('libs/additionnal.jar')
    compile 'com.android.support:appcompat-v7:25.1.1'
    compile project(':lvl_library')
}
