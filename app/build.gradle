apply plugin: 'com.android.application'

android {
    signingConfigs {
        westvansurfreport {
            keyAlias 'WVS'
            keyPassword '1Tangynorwvs'
            storeFile file('C:/Users/Admin/AndroidStudioProjects/WestVanSurfReport Keystore/westvansurfkey.jks')
            storePassword '1Tabbynorwvs'
        }
    }
    compileSdkVersion 21
    buildToolsVersion '21.1.1'
    defaultConfig {
        applicationId "com.rsnorrena.westvansurfreport"
        minSdkVersion 15
        targetSdkVersion 19
        versionCode 7
        versionName '7'
        signingConfig signingConfigs.westvansurfreport
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_7
        targetCompatibility JavaVersion.VERSION_1_7
    }
    productFlavors {
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile 'com.android.support:appcompat-v7:21.0.3'
    compile files('libs/jsoup-1.8.1.jar')
    compile files('libs/activation.jar')
    compile files('libs/mail.jar')
    compile project(':LicenseVerificationLibrary')
    compile files('libs/additionnal.jar')
}
